server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/revplay_favorites_db
    username: revature
    password: dharam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.security: DEBUG
    com.revplay: DEBUG