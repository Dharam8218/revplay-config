server:
  port: 8084

spring:
  application:
    name: FAVORITES-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/revplay_favorites_db
    username: revature
    password: dharam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true


logging:
  level:
    org.springframework.security: DEBUG
    com.revplay: DEBUG