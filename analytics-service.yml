server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/revplay_analytics_db
    username: revature
    password: dharam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    org.springframework.security: DEBUG