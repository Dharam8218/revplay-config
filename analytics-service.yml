server:
  port: 8087

spring:
  application:
    name: ANALYTICS-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/revplay_analytics_db
    username: revature
    password: dharam

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    org.springframework.security: DEBUG